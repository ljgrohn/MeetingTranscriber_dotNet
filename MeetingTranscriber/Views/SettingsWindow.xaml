<Window x:Class="MeetingTranscriber.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        mc:Ignorable="d"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Settings"
        Height="300"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Settings"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Margin="0,0,0,30"/>

        <!-- API Key Section -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="AssemblyAI API Key"
                       FontWeight="SemiBold"
                       Margin="0,0,0,10"/>

            <PasswordBox x:Name="ApiKeyPasswordBox"
                         Height="35"
                         FontSize="14"
                         PasswordChanged="ApiKeyPasswordBox_PasswordChanged"/>

            <TextBlock Text="Your API key is stored securely on your local machine"
                       FontSize="11"
                       Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                       Margin="0,5,0,0"/>

            <TextBlock Margin="0,10,0,0">
                <Hyperlink NavigateUri="https://www.assemblyai.com/dashboard/signup"
                           RequestNavigate="Hyperlink_RequestNavigate">
                    Don't have an API key? Sign up for free at AssemblyAI
                </Hyperlink>
            </TextBlock>
        </StackPanel>

        <!-- Status Message -->
        <TextBlock Grid.Row="2"
                   Text="{Binding StatusMessage}"
                   Foreground="{Binding StatusColor}"
                   FontWeight="SemiBold"
                   VerticalAlignment="Top"
                   TextWrapping="Wrap"
                   Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Width="100"
                    Height="35"
                    Margin="0,0,10,0"/>
            <Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource AccentButtonStyle}"
                    Width="100"
                    Height="35"/>
        </StackPanel>
    </Grid>
</Window>
